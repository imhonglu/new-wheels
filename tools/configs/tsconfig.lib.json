{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM"],
    "declaration": true,
    "declarationMap": true,
    "inlineSourceMap": true,
    "outDir": "${configDir}/dist",
    "baseUrl": "${configDir}"
  },

  "include": ["${configDir}/**/*.ts", "types/*.d.ts"]
}
